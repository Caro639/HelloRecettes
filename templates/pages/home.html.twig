{% extends "base.html.twig" %}

{% block title %}MyHome Accueil
{% endblock %}

{% block body %}
	<div class="container mt-4">

	{% for message in app.flashes('success') %}
				<div class="alert alert-success mt-4">
					{{ message }}
				</div>
	{% endfor %}

		<div class="hero-section">
			<h1 class="display-4">Bienvenue sur <span class="text-uppercase">hell</span><span class="text-orange">o</span><span class="text-uppercase">recettes</span> !</h1>
			<p class="lead">Créez, partagez et savourez ! HelloRecettes est votre compagnon culinaire qui vous permet de créer des recettes délicieuses à partir de vos propres ingrédients. Partagez vos créations avec la communauté ou gardez-les secrètes, c'est vous qui décidez !
			</p>
			<hr class="my-4">
			<p style="font-size: 1.1rem;">Commencez votre aventure culinaire dès maintenant !
			</p>
			<a class="btn btn-orange btn-lg" href="{{ path('app_security.registration') }}" role="button">Inscription</a>
		</div>

		<div class="recipes mt-5">
			<h2 class="section-title">Recettes de la communauté</h2>
			<div class="row mt-4">
				{% for recipe in recipes %}
					<div class="col-md-4 mb-4">
						<div class="card recipe-card">
							<div class="card-header">Recette n°{{ recipe.id }}</div>
							<div class="card-body d-flex flex-column">
								<h4 class="card-title">{{ recipe.name }}</h4>
								<div class="text-center my-3">
									{% if recipe.image %}
										<img class="img-fluid recipe-image" style="object-fit: cover; width: 100%; height: 200px;" src="{{ asset('../image/products/'~ recipe.image) }}" alt="Photo de la recette"/>
									{% else %}
										<img class="img-fluid recipe-image" style="object-fit: cover; width: 100%; height: 200px;" src="{{ asset('../image/products/replace.jpg') }}" alt="Photo par défaut de la recette"/>
									{% endif %}
								</div>
								<p class="card-text flex-grow-1" style="color: #b0b0b0;">{{ recipe.description|slice(0, 100) ~ '...' }}</p>
								<a href="{{ path('recipe.show', { id: recipe.id }) }}" class="btn btn-outline-orange mt-auto">
									🍽️ Voir la recette
								</a>
							</div>
						</div>
					</div>
				{% endfor %}
			</div>
		</div>
	</div>
{% endblock %}
