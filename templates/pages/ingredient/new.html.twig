{% extends "base.html.twig" %}

{% block title %}HelloRecettes - Nouveau ingrédient
{% endblock %}

{% block body %}
<div class="container mt-4 mb-5">
	<div class="row justify-content-center">
		<div class="col-lg-6">
			<div class="form-card">
				<div class="form-header">
					<div class="form-icon">🛒</div>
					<h2 class="form-title">Créer un ingrédient</h2>
					<p class="form-subtitle">Ajoutez un nouvel ingrédient à votre garde-manger</p>
				</div>

				{{ form_start(form, {'attr': {'class': 'custom-form'}}) }}

				<div class="mb-4">
					<label class="form-label">📝 Nom de l'ingrédient</label>
					{{ form_widget(form.name, {'attr': {'class': 'form-control form-input', 'placeholder': 'Ex: Tomates'}}) }}
					{{ form_errors(form.name) }}
				</div>

				<div class="mb-4">
					<label class="form-label">💰 Prix (€)</label>
					{{ form_widget(form.price, {'attr': {'class': 'form-control form-input', 'placeholder': '2.50'}}) }}
					{{ form_errors(form.price) }}
					<small class="form-text text-muted">Prix unitaire ou au kilo selon votre préférence</small>
				</div>

				{{ form_row(form._token) }}

				<div class="form-actions">
					<a href="{{ path('app_ingredient') }}" class="btn btn-cancel">Annuler</a>
					{{ form_widget(form.submit, {'attr': {'class': 'btn btn-submit'}, 'label': '✓ Créer l\'ingrédient'}) }}
					</div>

					{{ form_end(form) }}
				</div>
			</div>
		</div>
	</div>
{% endblock %}
