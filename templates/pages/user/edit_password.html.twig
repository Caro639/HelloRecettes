{% extends 'base.html.twig' %}

{% block title %}HelloRecettes - Modification du mot de passe
{% endblock %}

{% block body %}
	<div class="container mt-4 mb-5">
		<div class="row justify-content-center">
			<div class="col-lg-6">
				<div class="form-card">
					<div class="form-header">
						<div class="form-icon">🔐</div>
						<h2 class="form-title">Modifier mon mot de passe</h2>
						<p class="form-subtitle">Changez votre mot de passe en toute sécurité</p>
					</div>

					{% for message in app.flashes('warning') %}
						<div class="alert alert-warning mb-4">
							{{ message }}
						</div>
					{% endfor %}

					{% for message in app.flashes('success') %}
						<div class="alert alert-success mb-4">
							{{ message }}
						</div>
					{% endfor %}

					{{ form_start(form, {'attr': {'class': 'custom-form'}}) }}

					<div class="mb-4">
						<label class="form-label">🔑 Mot de passe actuel</label>
						{{ form_widget(form.plainPassword.first, {'attr': {'class': 'form-control form-input', 'placeholder': 'Votre mot de passe actuel'}}) }}
						{{ form_errors(form.plainPassword.first) }}
					</div>

					<div class="mb-4">
						<label class="form-label">🔒 Confirmation du mot de passe actuel</label>
						{{ form_widget(form.plainPassword.second, {'attr': {'class': 'form-control form-input', 'placeholder': 'Confirmez votre mot de passe actuel'}}) }}
						{{ form_errors(form.plainPassword.second) }}
					</div>

					<hr class="my-4" style="border-color: #3a3f44;">

					<div class="mb-4">
						<label class="form-label">🆕 Nouveau mot de passe</label>
						{{ form_widget(form.newPassword, {'attr': {'class': 'form-control form-input', 'placeholder': 'Votre nouveau mot de passe'}}) }}
						{{ form_errors(form.newPassword) }}
						<small class="form-text text-muted">Minimum 8 caractères recommandés</small>
					</div>

					{{ form_row(form._token) }}

					<div class="form-actions">
						<a href="{{ path('home.index') }}" class="btn btn-cancel">Annuler</a>
						{{ form_widget(form.submit, {'attr': {'class': 'btn btn-submit'}, 'label': '✓ Changer mon mot de passe'}) }}
					</div>

					{{ form_end(form) }}
				</div>
			</div>
		</div>
	</div>
{% endblock %}
