{% extends 'base.html.twig' %}

{% block title %}HelloRecettes - Recettes de la communauté
{% endblock %}

{% block body %}
	<div class="container mt-4 mb-5">
		<div class="page-header-section">
			<div class="page-icon">🍽️</div>
			<h1 class="page-title">Recettes de la communauté</h1>
			<p class="page-subtitle">Découvrez les créations culinaires partagées par nos membres</p>
		</div>

		{% if not recipes.items is same as([]) %}
			<div class="recipes-count">
				<span class="badge-count">{{ recipes.getTotalItemCount }}
					recette{{ recipes.getTotalItemCount > 1 ? 's' : '' }}</span>
			</div>

			<div class="row mt-4">
				{% for recipe in recipes %}
					<div class="col-md-6 col-lg-4 mb-4">
						<div class="recipe-card">
							<div class="card-header">
								<span class="recipe-badge">Recette n°{{ recipe.id }}</span>
							</div>
							<div class="card-body d-flex flex-column">
								<h4 class="card-title">{{ recipe.name }}</h4>
								<div class="text-center my-3">
									{% if recipe.image %}
										<img class="img-fluid recipe-image" style="object-fit: cover; width: 100%; height: 200px;" src="{{ asset('../image/products/'~ recipe.image) }}" alt="Photo de la recette"/>
									{% else %}
										<img class="img-fluid recipe-image" style="object-fit: cover; width: 100%; height: 200px;" src="{{ asset('../image/products/replace.jpg') }}" alt="Photo par défaut de la recette"/>
									{% endif %}
								</div>
								<p class="card-text flex-grow-1" style="color: #b0b0b0;">{{ recipe.description|slice(0, 100) ~ '...' }}</p>
								<a href="{{ path('recipe.show', { id: recipe.id }) }}" class="btn btn-outline-orange mt-auto">
									🍽️ Voir la recette
								</a>
							</div>
						</div>
					</div>
				{% endfor %}
			</div>

			<div class="pagination-wrapper">
				{{ knp_pagination_render(recipes) }}
			</div>
		{% else %}
			<div class="empty-state">
				<div class="empty-icon">📭</div>
				<h3>Aucune recette disponible</h3>
				<p>Soyez le premier à partager une délicieuse recette avec la communauté !</p>
			</div>
		{% endif %}
	</div>
{% endblock %}
