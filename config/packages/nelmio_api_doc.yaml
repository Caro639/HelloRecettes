nelmio_api_doc:
    documentation:
        info:
            title: Recettes
            description: Cette API vous propose les données de nos recettes partagées de nos utilisateurs!
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
           
